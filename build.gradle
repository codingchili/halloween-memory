apply plugin: 'java'

repositories {
    mavenCentral()
    jcenter()
}

jar {
    manifest {
        attributes(
                'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
                'Main-Class': 'com.codingchili.halloween.Main'
        )
    }
}

dependencies {
}